% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_goldstd_data.R
\name{loadGoldStdData}
\alias{loadGoldStdData}
\title{Load a cleaned gold-standard metadata file}
\usage{
loadGoldStdData(
  dataset = c("host", "host_ncbi", "isolation_source", "drug_class", "geography")
)
}
\arguments{
\item{dataset}{One of:
\itemize{
\item \code{"host"} -- cleaned host metadata, cross-checked against BV-BRC
\item \code{"host_ncbi"} -- cleaned host metadata, NCBI-only (no BV-BRC cross-check)
\item \code{"isolation_source"} -- LLM + human-reviewed isolation-source groups
\item \code{"drug_class"} -- AMR drug-name and drug-class normalization
\item \code{"geography"} -- cleaned country/location strings
}}
}
\value{
A tibble.
}
\description{
Reads one of the cleaned metadata tables shipped in \verb{inst/extdata/}
and returns it as a data frame. Raw pulls, regex/dictionary
crosswalks, and reference schema files are not exposed by this
function -- only cleaned metadata. Review provenance (human vs.
automated) varies by dataset and is noted per entry below where
confirmed. Source is BV-BRC by default; any other source (e.g. NCBI)
is called out explicitly in the dataset name.
}
\examples{
host <- loadGoldStdData("host")
head(host)
}
